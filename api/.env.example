# Application
DEBUG=true

# Server
API_HOST=0.0.0.0
API_PORT=8000

# CORS (comma-separated list)
CORS_ORIGINS=http://localhost:8080,http://localhost:5173

# Database - Docker Compose
# Using 127.0.0.1 instead of localhost to avoid GSSAPI authentication issues on macOS
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/demand_navigator?gssencmode=disable

# Alternative for connecting from another container:
# DATABASE_URL=postgresql://postgres:postgres@db:5432/demand_navigator
